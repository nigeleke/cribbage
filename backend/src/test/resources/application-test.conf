include "jdbc-read-journal.conf"

akka {
  event-handlers = ["akka.event.slf4j.Slf4jEventHandler"]
  loglevel = "DEBUG"
  stdout-loglevel = "DEBUG"

  actor {
    debug {
      receive = on
      autoreceive = on
#      lifecycle = on
      fsm = on
#      event-stream = on
#      unhandled = on
#      router-misconfiguration = on
    }
  }
}
