[package]
name = "desktop"
version = "0.1.0"
edition = "2024"

[dependencies]
dioxus = { workspace = true, features = ["router"] }
ui = { path = "../ui" }

[features]
default = []
desktop = ["dioxus/desktop"]
server = ["dioxus/server"]
