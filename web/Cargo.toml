[package]
name = "web"
version = "0.1.0"
edition = "2024"

[dependencies]
dioxus = { workspace = true, features = ["router"] }
ui = { path = "../ui" }

[features]
default = []
web = ["dioxus/web"]
server = ["dioxus/server"]
